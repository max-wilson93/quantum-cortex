# Template only — DO NOT commit real values. Create the real secret with:
#   kubectl create secret generic quantum-cortex-uw-secrets \
#     --from-literal=SUPABASE_URL=... \
#     --from-literal=SUPABASE_SERVICE_ROLE_KEY=... \
#     --from-literal=INGEST_SHARED_SECRET=...
apiVersion: v1
kind: Secret
metadata:
  name: quantum-cortex-uw-secrets
type: Opaque
stringData:
  SUPABASE_URL: "https://<project>.supabase.co"
  SUPABASE_SERVICE_ROLE_KEY: "<service-role-key>"
  INGEST_SHARED_SECRET: "<same value as PTM's INGEST_SHARED_SECRET>"
